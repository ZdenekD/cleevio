const variables = `
    :root {
        --transition-duration-in: .3s;
        --transition-duration-out: .2s;
        --transition-timing: var(--ease-out-quart);
        --border-radius: 10px;
        --button-min-width: 200px;
        --zindex-nav: 96;
        --zindex-logo: 97;
        --zindex-nav-trigger: 98;
        --zindex-overlay: 99;
        --zindex-modal: 100;
        --header-height: 90px;
    }
`;

export default variables;
