const variables = `
    :root {
        --transition-duration-in: .3s;
        --transition-duration-out: .2s;
        --transition-timing: var(--ease-out-quart);
        --border-radius: 10px;
        --button-min-width: 200px;
    }
`;

export default variables;
